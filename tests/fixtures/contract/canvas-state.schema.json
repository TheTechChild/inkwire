{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/TheTechChild/inkwire/schema/canvas-state.schema.json",
  "title": "Inkwire CanvasState",
  "description": "Return shape of canvas.get_state. Meaning (graph) and placement (layout) are separate fields with independent revision counters.",
  "type": "object",
  "required": ["board", "graph", "layout", "ink", "images", "history", "viewport", "layers", "focus", "drafts", "active_draft"],
  "additionalProperties": false,
  "properties": {
    "board": {
      "type": "object",
      "required": ["id", "name"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" }
      }
    },
    "graph": {
      "type": "object",
      "description": "What the diagram means. Positions are deliberately absent.",
      "required": ["revision", "nodes", "edges"],
      "additionalProperties": false,
      "properties": {
        "revision": {
          "type": "integer",
          "minimum": 0,
          "description": "Increments on any change to node or edge semantics. Unchanged by moves."
        },
        "nodes": { "type": "array", "items": { "$ref": "#/$defs/node" } },
        "edges": { "type": "array", "items": { "$ref": "#/$defs/edge" } },
        "boundary_edges": {
          "type": "array",
          "description": "Scoped reads only. Edges with exactly one endpoint in the focused layer.",
          "items": { "$ref": "#/$defs/boundaryEdge" }
        },
        "boundary_nodes": {
          "type": "array",
          "description": "Scoped reads only. The far endpoint of every boundary edge, as a stub.",
          "items": { "$ref": "#/$defs/boundaryNode" }
        }
      }
    },
    "layout": {
      "type": "object",
      "description": "Where things sit. Bumped by drags and by canvas.move only.",
      "required": ["revision", "units", "boxes"],
      "additionalProperties": false,
      "properties": {
        "revision": { "type": "integer", "minimum": 0 },
        "units": { "const": "canvas px" },
        "boxes": {
          "type": "object",
          "description": "Element id to [x, y, w, h]. Covers nodes and images.",
          "additionalProperties": {
            "type": "array",
            "items": { "type": "number" },
            "minItems": 4,
            "maxItems": 4
          }
        }
      }
    },
    "ink": {
      "type": "array",
      "description": "Freehand strokes not yet resolved into structure. Full polylines only when include_ink_geometry is set.",
      "items": { "$ref": "#/$defs/stroke" }
    },
    "images": { "type": "array", "items": { "$ref": "#/$defs/image" } },
    "history": { "$ref": "#/$defs/historySummary" },
    "viewport": {
      "type": "object",
      "required": ["x", "y", "zoom"],
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "zoom": { "type": "number", "minimum": 0.35, "maximum": 2.4 }
      }
    },
    "layers": {
      "type": "array",
      "description": "Every layer on the board. Always present, whole-board and scoped.",
      "items": { "$ref": "#/$defs/layer" }
    },
    "focus": {
      "type": ["string", "null"],
      "description": "Layer id the human is looking at, shared by every panel on the board. Null when released."
    },
    "drafts": {
      "type": "array",
      "description": "Every draft on the board, whole even on a scoped read — a draft is an overlay, not a cut.",
      "items": { "$ref": "#/$defs/draft" }
    },
    "active_draft": {
      "type": ["string", "null"],
      "description": "Draft id the human is looking at, shared by every panel on the board. Null when none is active."
    },
    "scope": {
      "type": "object",
      "description": "Present only when the read is scoped to a focused layer. omitted is mandatory — counts, not ids.",
      "required": ["layer_id", "letter", "title", "note", "omitted", "whole_board", "paths"],
      "additionalProperties": false,
      "properties": {
        "layer_id": { "type": "string" },
        "letter": { "type": "string" },
        "title": { "type": "string" },
        "note": { "type": "string" },
        "omitted": {
          "type": "object",
          "required": ["nodes", "edges"],
          "additionalProperties": false,
          "properties": {
            "nodes": { "type": "integer", "minimum": 0, "description": "Total nodes minus members." },
            "edges": { "type": "integer", "minimum": 0, "description": "Total edges minus internal minus boundary." }
          }
        },
        "whole_board": { "const": "canvas_get_board", "description": "The tool that returns the whole board regardless of focus." },
        "paths": {
          "type": "array",
          "items": { "$ref": "#/$defs/path" },
          "description": "The focused layer's paths, full steps."
        }
      }
    }
  },
  "$defs": {
    "author": {
      "type": "string",
      "enum": ["human", "ai"]
    },
    "node": {
      "type": "object",
      "required": ["id", "label", "kind", "ref", "endpoint", "author"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "label": { "type": "string" },
        "kind": {
          "type": "string",
          "enum": ["entry", "service", "store", "transform", "note"],
          "description": "See SPEC.md open question 1 — state and lifeline are likely additions."
        },
        "ref": {
          "type": ["string", "null"],
          "description": "path/to/file.ts or path/to/file.ts:symbol. Validated against the project root on write."
        },
        "endpoint": { "type": ["string", "null"] },
        "from_ink": {
          "type": ["array", "null"],
          "items": { "type": "string" },
          "description": "Stroke ids this node was inferred from. Provenance for either inference path."
        },
        "author": { "$ref": "#/$defs/author" }
      }
    },
    "edge": {
      "type": "object",
      "required": ["id", "from", "to", "kind", "author"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "from": { "type": "string", "description": "Node id. Guaranteed to exist — the fold prunes dangling edges." },
        "to": { "type": "string" },
        "label": { "type": ["string", "null"], "description": "Short name for the transition, e.g. \"miss\"." },
        "schema": { "type": ["string", "null"], "description": "Payload type crossing this edge, e.g. \"OrderDTO\"." },
        "kind": { "type": "string", "enum": ["sync", "async", "error"] },
        "condition": { "type": ["string", "null"], "description": "Branch predicate, e.g. \"cache miss\"." },
        "from_ink": { "type": ["array", "null"], "items": { "type": "string" } },
        "author": { "$ref": "#/$defs/author" }
      }
    },
    "stroke": {
      "type": "object",
      "required": ["id", "bbox"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "points": {
          "type": "integer",
          "description": "Point count. Present unless include_ink_geometry was requested."
        },
        "geometry": {
          "type": "array",
          "description": "Full polyline in canvas px, draw order. Only with include_ink_geometry.",
          "items": {
            "type": "array",
            "items": { "type": "number" },
            "minItems": 2,
            "maxItems": 2
          }
        },
        "bbox": {
          "type": "object",
          "required": ["x", "y", "w", "h"],
          "additionalProperties": false,
          "properties": {
            "x": { "type": "number" }, "y": { "type": "number" },
            "w": { "type": "number" }, "h": { "type": "number" }
          }
        },
        "author": { "$ref": "#/$defs/author" }
      }
    },
    "image": {
      "type": "object",
      "required": ["id", "src", "natural", "author"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "src": { "type": "string", "description": "Server-relative URL of the stored bitmap." },
        "natural": {
          "type": "array",
          "items": { "type": "integer" },
          "minItems": 2,
          "maxItems": 2
        },
        "author": { "$ref": "#/$defs/author" }
      }
    },
    "layer": {
      "type": "object",
      "description": "A named subset of nodes. A view over the board, never a container — deleting one deletes nothing.",
      "required": ["id", "letter", "title", "note", "nodes", "author", "paths"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "letter": { "type": "string", "description": "A–Z, first unused; \"?\" once all 26 are taken." },
        "title": { "type": "string", "maxLength": 24 },
        "note": { "type": "string", "description": "The AI's one-line rationale; shown on focus." },
        "nodes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Node ids (notes are nodes). Never edges, never ink."
        },
        "author": { "$ref": "#/$defs/author" },
        "paths": {
          "type": "array",
          "items": { "$ref": "#/$defs/path" },
          "description": "Ordered walks over the layer's edges. [] for boards that predate paths."
        }
      }
    },
    "path": {
      "type": "object",
      "description": "An ordering over the layer, never a second copy of the graph: edge ids only. Nodes are derived from the edges.",
      "required": ["id", "title", "steps", "author"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "description": "P1, P2… first unused across the board." },
        "title": { "type": "string", "maxLength": 24 },
        "steps": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/pathStep" } },
        "author": { "$ref": "#/$defs/author" }
      }
    },
    "pathStep": {
      "type": "object",
      "description": "One hop: an edge inside the layer, the caption the human reads while it plays, and its citation.",
      "required": ["edge", "caption", "ref"],
      "additionalProperties": false,
      "properties": {
        "edge": { "type": "string", "description": "Edge id, internal to the layer." },
        "caption": { "type": "string", "maxLength": 160, "description": "\"\" when absent." },
        "ref": { "type": ["string", "null"], "description": "path/to/file.ts:symbol, validated like bind_code." }
      }
    },
    "draft": {
      "type": "object",
      "description": "A proposed change and what it touches. A view like a layer — nothing on the board changes when a draft is created, marked or activated.",
      "required": ["id", "title", "note", "marks", "author"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "description": "D1, D2… first unused across the board." },
        "title": { "type": "string", "maxLength": 24 },
        "note": { "type": "string", "description": "What the change is and why. \"\" when absent." },
        "marks": {
          "type": "object",
          "description": "Element id (node or edge) → role. Explicit: marking a node marks none of its edges.",
          "additionalProperties": { "type": "string", "enum": ["removed", "changed", "added"] }
        },
        "author": { "$ref": "#/$defs/author" }
      }
    },
    "boundaryEdge": {
      "type": "object",
      "required": ["id", "from", "to", "label", "kind", "out_of_scope", "crosses_to"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "from": { "type": "string" },
        "to": { "type": "string" },
        "label": { "type": ["string", "null"] },
        "kind": { "type": "string", "enum": ["sync", "async", "error"] },
        "out_of_scope": { "const": true },
        "crosses_to": { "type": "string", "description": "The endpoint outside the layer." }
      }
    },
    "boundaryNode": {
      "type": "object",
      "description": "Stub only — no ref, no endpoint, no layout.",
      "required": ["id", "label", "kind", "stub"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "label": { "type": "string" },
        "kind": { "type": "string", "enum": ["entry", "service", "store", "transform", "note", "state", "lifeline"] },
        "stub": { "const": true }
      }
    },
    "historySummary": {
      "type": "object",
      "description": "In-memory, per server session. Boards persist; timelines do not.",
      "required": ["steps", "head", "applied", "skipped", "ahead", "conflicts"],
      "additionalProperties": false,
      "properties": {
        "steps": { "type": "integer", "minimum": 0, "description": "Total steps excluding the base." },
        "head": { "type": "integer", "minimum": 0, "description": "Timeline pointer. The board is the fold of steps 0..head." },
        "applied": { "type": "integer", "minimum": 0 },
        "skipped": { "type": "integer", "minimum": 0 },
        "ahead": { "type": "integer", "minimum": 0, "description": "Steps past the head, replayable forward." },
        "conflicts": { "type": "integer", "minimum": 0 },
        "edges_pruned": { "type": "integer", "minimum": 0, "description": "Dangling edges removed by the fold's integrity pass." },
        "by_human": { "type": "integer", "minimum": 0 },
        "by_ai": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
